[pytest]
minversion = 6.0
addopts = -ra -q --cov=cfdiag --cov-report=term-missing --cov-fail-under=50
testpaths =
    tests
markers =
    unit: purely functional tests (no I/O)
    integration: tests that mock I/O boundaries
    slow: tests that take longer than 1s
